document.addEventListener('DOMContentLoaded', function() {
    // Simula un proceso de carga (reemplaza con tu lógica de carga real)
    setTimeout(function() {
        // Redirige a la página del contenido principal
        window.location.href = 'carta.html';
    }, 7000); // Ajusta el tiempo según sea necesario
});